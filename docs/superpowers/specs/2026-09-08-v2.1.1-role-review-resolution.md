# v2.1.1 Role Review Resolution

Date: 2026-09-08

This document resolves the role-specific follow-up reviews after v2.1 / PR #3.

## XXT_MATHEMATICAL review

Disposition: ACCEPTED.

Resolved through PR #4 and merged before this branch:

- `CORE-MATH-AUTH-001` Mathematical Veto;
- `G3_FREEZE` requires current `G2_VALIDATION=PASS`;
- current-version Mathematical Review provenance is required;
- `NEAR-OPTIMAL` requires quantitative optimality evidence rather than strong baseline alone;
- dynamic/path-dependent hard constraints require full replay;
- material surrogate/proxy fidelity failure may trigger `MATHEMATICAL_P0` and `P0 REOPEN`;
- exact/MILP anchor remains Claim- and budget-dependent rather than mechanically mandatory.

## CYQ_PAPER review

Disposition: ACCEPTED WITH MINIMAL PATCH.

### P0-1 Technical facts read-only

Resolution:

- added `CORE-PAPER-AUTH-001`;
- CYQ may read, cite, detect conflicts and send `reopen request`;
- CYQ may not modify mathematical model, objective, hard constraints, frozen parameters, units, technical accounting, formal metrics or frozen numbers;
- technical changes return to FYQ / XXT and require re-review/re-freeze before paper use.

### P0-2 Paper Handoff self-description

Resolution:

`templates/Paper_Handoff模板.md` now requires:

- key parameter value, provenance, reason, scan range and stability range;
- iteration/convergence facts including per-iteration metrics, accept/reject reason and scenario convergence differences;
- core metric semantics including definition/formula, unit, statistical scope, baseline, direction and frozen value.

The target behavior is that CYQ can draft from the Handoff without reopening source code or chat history for basic technical meaning.

### P0-3 AI disclosure blocking Gate

Resolution:

- added `CORE-AI-DISCLOSURE-001`;
- for `adopted=yes` items entering paper/code/figures/core claims, `human_verification`, `team_decision`, `human_changes`, `paper_location`, and `artifact` are required;
- missing required evidence produces `AI Disclosure Gate FAIL`;
- `templates/gates.json` now requires `ai_disclosure_gate_pass` before submission.

### P1-1 Figure window provenance

Resolution:

Figure Registry now includes:

- `time_scope`;
- `window_rule`;
- `selection_reason`.

Window selection must be objective and auditable; `FULL_HORIZON` and `NOT_APPLICABLE` are explicit options.

### P1-2 Behavioral contract tests

Resolution:

Added `tests/test_cyq_paper_hardening_contract.py` covering:

- CYQ technical-fact read-only behavior;
- Handoff parameter/convergence/metric semantics;
- Figure Registry window provenance;
- blocking AI disclosure Gate.

The CI workflow was also corrected to run test discovery rather than only one hard-coded test module.

## TDD evidence

RED:

- workflow run #56;
- head `44776a7c859f73b0bd3c21d3b729817580385294`;
- Unit contract tests failed after enabling test discovery and before implementation.

GREEN:

- workflow run #66;
- head `fd33cfee92ec9b83353a00c48309f9bb41febeff`;
- Unit contract tests PASS;
- canonical runtime validator PASS;
- Runtime Lite build PASS;
- built Runtime Lite validator PASS;
- forbidden corpus binary scan PASS.

## Merge condition

This branch is technically ready for CYQ role-specific regression review. Merge should occur only after that review confirms the original CYQ P0/P1 findings are resolved without creating a new Paper Lead authority conflict.
