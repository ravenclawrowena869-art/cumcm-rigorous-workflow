# Q4 Paper Handoff — FINAL_FROZEN R2

Consumer: CYQ Paper lane.

Use `Q4_PAPER_NUMBER_REGISTRY_R2.csv` as the only Q4 numeric registry for paper prose/tables. Use `Q4_FIGURE_REGISTRY_FINAL_R2.csv` as the frozen figure registry. Figure binaries/source assets remain in the sealed Q4 archive and are copied into `competition/figures/` only when the paper lane selects them.

Any model, code, settlement-semantic, or formal-number change requires reopening the Q4 freeze; ordinary prose/layout edits do not.
