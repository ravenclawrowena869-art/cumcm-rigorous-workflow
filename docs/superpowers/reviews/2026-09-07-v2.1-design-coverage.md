# v2.1 Design Coverage Review

本文件在创建 PR 前逐项对照两份设计规格。

## Shared Three-GPT 验收 12 项

1. 同一 Runtime Skill 存在 FYQ / XXT / CYQ 三 Profile：`skills/.../profiles/`。
2. `SKILL.md` 按 `ACTIVE_ROLE` 路由：已实现。
3. 未绑定角色不猜：dispatcher 与 `AGENTS.md` 明确 role-neutral read-only / 一次询问。
4. Shared Core 对三个 Profile 均生效：`core/SHARED_CORE.md` + dispatcher 非覆盖规则。
5. FYQ Profile 输出 Mathematical Review Request 与 Paper Handoff：已定义。
6. XXT Profile 输出 MODEL_SPEC、validator、anchor/bound、数学 Review：已定义。
7. CYQ Profile 证据不足阻断可定稿正文：`INCOMPLETE + 缺口清单`。
8. 三 Profile 共用 `project_state.yaml`：模板增加 `agent_bindings`、handoff 与 evidence gate 字段。
9. `.cumcm-agent.local.yaml` 被 `.gitignore`：已实现，并提供 example。
10. PR / contribution governance：`06_三GPT协作与Skill共同维护.md`。
11. Skill 更新不重新打包大型论文：Runtime Lite builder + corpus policy。
12. 静态测试：`tests/test_runtime_skill_contract.py` + validator + GitHub Actions。

## 原 v2.1 验收 10 项

1. README 明确 Orchestrator 与三人长期角色：已实现。
2. 协作目录有 Orchestrator / Handoff：Profile + 分工文档 + Handoff 规范。
3. 评委反馈 Gate：模型证据 Gate v2.1。
4. 论文目录有目的—收益—证据、本问专属流程和稳健性要求：写作 Gate v2.1。
5. AI 目录有 Ledger 与四阶段法：已实现。
6. Runtime Lite 不含大型 PDF/PNG：manifest / builder / tests / CI。
7. Full Archive 策略：`docs/runtime-lite-corpus-policy.md`。
8. 任务单、Paper Handoff、Robustness Plan、AI Ledger 模板：已实现。
9. 静态验证脚本：已实现。
10. feature branch + PR 交付：feature branch 已使用，PR 为下一步。

## CYQ Q4 主控指令差异裁决

与 Shared Skill 的理解总体一致。保留三个边界：

- FYQ 统一集成 Q4，但需要 XXT Mathematical PASS；
- “XXT 不另行设计第二套主模型”只适用于本次冻结演练，数学 P0 仍可提出最小替代/重开；
- 10/20/30%、RegionA、20% cap 与三张指定图是 Q4 case 规则，不写入 Shared Core。

专项 authority 已保存于 `09_本次比赛复盘/04_Q4赛前闭环演练Authority.md`。
