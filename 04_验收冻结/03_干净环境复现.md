# 干净环境复现

冻结前做一次真正的 clean replay。

## 做法

1. 新建空目录；
2. 只复制 / 解压最终 ZIP；
3. 不允许访问原工程路径；
4. 安装声明依赖；
5. 从官方输入或包内指定输入运行；
6. 生成最终输出；
7. 比较 hash / 指标；
8. 删除缓存后再确认一次。

## 常见失败

- 代码读取了绝对路径；
- 依赖旧 checkpoint；
- 从外部文件夹偷读 CSV；
- 图脚本读取旧结果；
- 输出目录会覆盖 frozen 文件；
- README 命令已过期。

只有 clean replay 通过，才叫“可复现”。
