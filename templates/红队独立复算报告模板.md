# 红队独立复算报告

## 1. 基本信息

- question/module:
- source_commit:
- main_model_version:
- input_manifest/hash:
- redteam_reviewer:
- review_time:

## 2. 信息隔离声明

### 已读取

- [ ] 官方题面 / 规则 / 附件
- [ ] Question Contract / 指标定义
- [ ] 原始数据或冻结输入
- [ ] 结果声明（数值、单位、样本范围、聚合口径）

### 未读取

- [ ] 主求解代码
- [ ] 主建模推导笔记
- [ ] 主结果目录中间产物
- [ ] 预期答案 / 仲裁结论 / 暗示正确数值的提示

若存在不得不共享的信息，说明原因及独立性损失：

> 

## 3. 复算设计

- 复算目标：
- 独立计算路径：
- 与主方法是否不同方法族：
- 关键公式来源：
- 使用数据范围：
- 单位：
- 时间窗口：
- 异常值处理：
- 随机种子 / 冻结输入：

## 4. 同声明口径复算

| claim_id / metric | 主结果 | 红队复算 | 单位 | 相对差 | 结果 |
|---|---:|---:|---|---:|---|
|  |  |  |  |  |  |

相对差建议使用：

```text
abs(main - replicated) / max(abs(main), eps)
```

具体容差应由指标数值精度、求解器精度、题目口径和模型结构决定，不机械统一为固定百分比。

## 5. 独立口径挑战

| metric | 声明口径 | 独立口径 | 独立结果 | 是否影响核心结论 |
|---|---|---|---:|---|
|  |  |  |  |  |

## 6. hard constraints / accounting 复核

- [ ] 目标值独立复算
- [ ] 变量范围检查
- [ ] hard constraints replay
- [ ] 单位 / 量纲检查
- [ ] accounting / metric 口径检查
- [ ] 动态约束全过程 replay（适用时）

最大违反量：

发生位置：

## 7. 分歧分类

每条分歧只使用以下类型之一：

- `NUMERIC`：同声明口径仍不能复现；
- `DEFINITION`：指标定义/统计口径存在差异；
- `NOT_COMPARABLE`：缺冻结输入、缺必要信息或结果不可比较；
- `NONE`：复算对齐。

| issue_id | metric | type | evidence | severity | required_action |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 8. 验收结论

只允许：

- `PASS`
- `PASS WITH NON-MODEL PATCH`
- `FAIL / REOPEN`

结论：

阻断原因（若有）：

需要重跑的依赖：

证据文件：

> 自动化流水线是否继续执行其他无依赖任务，与本验收结论分开记录。不得因超时、预算不足或重试耗尽把 FAIL 自动改成 PASS。
