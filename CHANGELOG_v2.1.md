# CHANGELOG v2.1

## v2.1.2 — 逐问求解章节短导语与结构收敛

本次增量不新建重复 Paper Skill，也不把规则继续堆进主 `SKILL.md`；核心规则落在 `05_论文与图表/05_逐问写作与算法呈现Gate.md`，dispatcher 只保留入口。

新增/调整：

- 当前文已有“问题分析”时，正式求解章节禁止再设置“本问目标与难点”等重复小节；
- 大标题下改用四步短导语：“任务概括 → 核心难点 → 模型选择 → 应用依据”；
- 模型必须由结构性难点自然引出，禁止先报“高级算法”再补理由；
- 导语禁止提前写变量、公式、算法步骤、正式数字和未冻结结果；
- 收束句必须指向具体决策、比较或判断，禁止空泛“重要支撑”；
- 推荐逐问结构更新为“短导语 → 建模依据 → 模型建立 → 求解算法与伪代码 → 结果及证据 → 本问小结”；
- 篇幅紧张可删本问小结，但不得删结果解释；
- Paper Pipeline、dispatcher、contract tests、runtime validator 与版本号同步更新至 2.1.2。

## Paper Evidence Gates

本版本吸收 2026 华数杯赛后指导教师批注与评委反馈，将经验从“提醒”升级为 AI 可执行的验收 Gate。

新增：

- 根目录 `SKILL.md`，使仓库可作为平台无关的数模工作流 Skill 使用；
- `04_验收冻结/05_模型证据充分性Gate.md`；
- `05_论文与图表/05_逐问写作与算法呈现Gate.md`。

强化：

- 每问标题后的问题概述；
- 与题目变量、约束和失败分支一致的伪代码/算法流程；
- 模型选择的场景化理由与备选比较；
- 基线、最优值锚点、输入重排、多种子、参数扫描和鲁棒性；
- 多目标迭代的服务质量、终止准则和不可行性解释；
- 表题、图题、交叉引用、文献角标、分页空白和标题跨行检查；
- AI 在证据不足时输出缺口，而不是用通用语言补齐正文。
